services:
  - type: web
    name: chuyen-de-thuc-tap-backend
    env: php
    rootDir: duongminhphuc_lar
    buildCommand: |
      composer install
      php artisan key:generate
      php artisan migrate --force
    startCommand: php artisan serve --host 0.0.0.0 --port 10000
